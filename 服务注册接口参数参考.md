# 服务注册接口参数完整参考

本文档列出服务注册与描述符管理所有 gRPC/HTTP 接口的完整请求与响应参数，包含所有嵌套对象的字段说明。

> **配套文档**：[业务接入指南](业务接入指南.md) — 快速接入流程与示例

---

## 接口权限总览

服务发现接口按权限分为三类：

| 权限级别 | 接口 | 说明 |
|----------|------|------|
| **管理端专属** | `InitService`, `RegisterService`, `DeregisterService`, `DeleteServiceRecord`, `UpdateServiceInstance`, `UploadServiceConfig` | 仅管理员可操作 |
| **业务侧可用** | `UploadProtobufDescriptor`, `RollbackDescriptor`, `UpdateServiceHealth` | 通过 `app_secret` (服务凭证) 认证，仅允许操作绑定的服务 |
| **只读开放** | `ListServices`, `GetServiceInstances`, `ServiceHealthCheck`, `ListProtobufDescriptors`, `GetProtobufDescriptor`, `ListDescriptorVersions`, `GetServiceConfig` | 所有认证用户可访问 |

---

## 目录

- [一、公共说明](#一公共说明)
- [二、InitService — 初始化服务（管理端专属）](#二initservice--初始化服务管理端专属)
- [三、RegisterService — 注册服务实例（管理端专属）](#三registerservice--注册服务实例管理端专属)
- [四、DeregisterService — 注销服务实例（管理端专属）](#四deregisterservice--注销服务实例管理端专属)
- [五、UpdateServiceHealth — 更新健康状态（业务侧可用）](#五updateservicehealth--更新健康状态业务侧可用)
- [六、UploadProtobufDescriptor — 上传描述符（业务侧可用）](#六uploadprotobuf descriptor--上传描述符业务侧可用)
- [七、RollbackDescriptor — 回滚描述符版本（业务侧可用）](#七rollbackdescriptor--回滚描述符版本业务侧可用)
- [八、ListDescriptorVersions — 列出版本历史](#八listdescriptorversions--列出版本历史)
- [九、嵌套对象定义](#九嵌套对象定义)
- [十、枚举值参考](#十枚举值参考)
- [十一、完整请求示例](#十一完整请求示例)

---

## 一、公共说明

### 认证

所有接口均需携带认证头，支持以下任意一种：

| 认证方式 | Header / Metadata |
|----------|-------------------|
| API Key | `x-api-key: <your_api_key>` |
| JWT Bearer | `authorization: Bearer <token>` |

### HTTP 端点基础路径

```
http[s]://<gateway_host>:<port>/api/v1/discovery/
```

### gRPC 服务完整名称

```
stew.api.v1.ServiceDiscoveryService
```

---

## 二、InitService — 初始化服务（管理端专属）

**权限**：管理员  
**gRPC 方法**：`InitService`  
**HTTP**：`POST /api/v1/discovery/services/init`

管理员初始化一个新服务，系统生成 `app_id` + `app_secret` 凭证供业务侧使用。

### 2.1 请求参数 `InitServiceRequest`

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `service_name` | `service_name` | `string` | 是 | — | 服务完整名称，如 `stew.api.v1.OrderService` |
| `description` | `description` | `string` | 否 | — | 服务的简要描述 |
| `protocol` | `protocol` | `string` | 否 | `grpc` | 协议类型：`grpc` / `http` |

### 2.2 响应参数 `InitServiceResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `success` | `success` | `bool` | 是否初始化成功 |
| `message` | `message` | `string` | 状态描述 |
| `app_id` | `app_id` | `string` | 服务凭证 ID，用于标识 |
| `app_secret` | `app_secret` | `string` | 服务凭证密钥（仅显示一次），业务侧通过 `x-api-key` 头传递 |
| `service_name` | `service_name` | `string` | 服务名称（回显） |

### 2.3 请求示例

```json
POST /api/v1/discovery/services/init
Authorization: Bearer <admin_token>

{
  "service_name": "stew.api.v1.OrderService",
  "description": "Order management service",
  "protocol": "grpc"
}
```

**响应**：

```json
{
  "success": true,
  "message": "Service initialized successfully",
  "app_id": "key_abc123",
  "app_secret": "ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "service_name": "stew.api.v1.OrderService"
}
```

> **重要**：`app_secret` 仅在创建时返回一次，请妥善保管。丢失后需重新初始化服务生成新凭证。

---

## 三、RegisterService — 注册服务实例（管理端专属）

**权限**：管理员  
**gRPC 方法**：`RegisterService`  
**HTTP**：`POST /api/v1/discovery/services/register`

### 2.1 请求参数 `RegisterServiceRequest`

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `service` | `service` | `ServiceInstance` | 是 | — | 服务实例信息，见 [ServiceInstance](#serviceinstance) |
| `ttl` | `ttl` | `uint32` | 否 | `60` | 注册 TTL（秒），超时未续约则自动注销 |

#### ServiceInstance

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `service_name` | `service_name` | `string` | 是 | — | 服务完整名称，如 `stew.api.v1.UserService` |
| `instance_id` | `instance_id` | `string` | 否 | 自动生成 | 实例唯一标识，同服务下不重复；留空则由网关生成 |
| `lb` | `lb` | `LoadBalancer` | 是 | — | 负载均衡配置，含端点列表，见 [LoadBalancer](#loadbalancer) |
| `version` | `version` | `string` | 否 | `1.0.0` | 服务版本号，用于标识及灰度 |
| `metadata` | `metadata` | `Struct (JSON object)` | 否 | `{}` | 任意 KV 元数据，可用于路由过滤 |
| `health_endpoint` | `health_endpoint` | `string` | 否 | — | 健康检查路径，如 `/health` |
| `health_check_config` | `health_check_config` | `HealthCheckConfig` | 否 | — | 健康检查详细配置，见 [HealthCheckConfig](#healthcheckconfig) |
| `status` | `status` | `ServiceStatus (enum)` | 否 | `HEALTHY` | 初始状态，见 [ServiceStatus](#servicestatus) |
| `weight` | `weight` | `uint32` | 否 | `1` | 负载均衡权重，越大分配流量越多 |
| `tags` | `tags` | `map<string, string>` | 否 | `{}` | 服务标签，支持按标签过滤路由 |
| `protocol` | `protocol` | `string` | 否 | `grpc` | 协议类型：`grpc` / `http` / `tcp` |
| `tls_enabled` | `tls_enabled` | `bool` | 否 | `false` | 是否启用 TLS |
| `protobuf_descriptor` | `protobuf_descriptor` | `bytes (base64)` | 否 | — | 编译好的 `.pb` 文件二进制内容，上传后网关自动加载路由 |
| `middleware_config` | `middleware_config` | `ServiceMiddlewareConfig` | 否 | — | 服务级中间件开关配置，见 [ServiceMiddlewareConfig](#servicemiddlewareconfig) |

### 2.2 响应参数 `RegisterServiceResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `success` | `success` | `bool` | 是否注册成功 |
| `message` | `message` | `string` | 状态描述 |
| `lease_id` | `lease_id` | `string` | ETCD 租约 ID，用于续约心跳 |

---

## 四、DeregisterService — 注销服务实例（管理端专属）

**权限**：管理员  
**gRPC 方法**：`DeregisterService`  
**HTTP**：`POST /api/v1/discovery/services/deregister`

### 3.1 请求参数 `DeregisterServiceRequest`

| 字段 | JSON Key | 类型 | 必填 | 说明 |
|------|----------|------|------|------|
| `service_name` | `service_name` | `string` | 是 | 服务完整名称 |
| `instance_id` | `instance_id` | `string` | 是 | 实例 ID（注册时返回或自行提供） |

### 3.2 响应参数 `DeregisterServiceResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `success` | `success` | `bool` | 是否注销成功 |
| `message` | `message` | `string` | 状态描述 |

---

## 五、UpdateServiceHealth — 更新健康状态（业务侧可用）

**权限**：业务侧（通过 `app_secret` 认证，需 `service:<service_name>:heartbeat` scope）  
**gRPC 方法**：`UpdateServiceHealth`  
**HTTP**：`POST /api/v1/discovery/services/health`

### 4.1 请求参数 `UpdateServiceHealthRequest`

| 字段 | JSON Key | 类型 | 必填 | 说明 |
|------|----------|------|------|------|
| `service_name` | `service_name` | `string` | 是 | 服务完整名称 |
| `instance_id` | `instance_id` | `string` | 是 | 实例 ID |
| `status` | `status` | `ServiceStatus (enum)` | 是 | 新的健康状态，见 [ServiceStatus](#servicestatus) |
| `health_message` | `health_message` | `string` | 否 | 可读的健康状态描述 |

### 4.2 响应参数 `UpdateServiceHealthResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `success` | `success` | `bool` | 是否更新成功 |
| `message` | `message` | `string` | 状态描述 |

---

## 六、UploadProtobufDescriptor — 上传描述符（业务侧可用）

**权限**：业务侧（通过 `app_secret` 认证，需 `service:<service_name>:upload_descriptor` scope）  
**gRPC 方法**：`UploadProtobufDescriptor`  
**HTTP**：`POST /api/v1/discovery/descriptors/{service_name}/upload`

### 5.1 请求参数 `UploadProtobufDescriptorRequest`

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `service_name` | `service_name` | `string` | 是 | — | 服务完整名称，必须与 `.pb` 中的 service package 前缀匹配 |
| `descriptor_data` | `descriptor_data` | `bytes (base64)` | 是 | — | `protoc --descriptor_set_out` 编译的二进制数据，JSON 传输时使用 base64 编码 |
| `descriptor_version` | `descriptor_version` | `string` | 否 | 自动生成 | 版本标识，留空则自动生成 `{timestamp}-{hash_prefix}` |
| `description` | `description` | `string` | 否 | — | 版本描述，便于回滚时识别 |
| `signature` | `signature` | `string` | 否 | — | `HMAC-SHA256(descriptor_data, shared_secret)`，可选完整性校验 |
| `force` | `force` | `bool` | 否 | `false` | `true` 则忽略兼容性警告强制更新 |
| `previous_version` | `previous_version` | `string` | 否 | — | 乐观锁：若当前 active 版本与此值不符则拒绝更新，防止并发冲突 |

> **幂等性**：若 `descriptor_data` 的 SHA-256 与当前 active 版本相同，网关直接返回 `success=true`，不触发热加载。

### 5.2 响应参数 `UploadProtobufDescriptorResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `success` | `success` | `bool` | 是否上传成功 |
| `message` | `message` | `string` | 状态描述 |
| `descriptor_key` | `descriptor_key` | `string` | ETCD 存储路径，如 `/descriptors/my-service/v/20260318120000-a1b2c3` |
| `discovered_services` | `discovered_services` | `string[]` | 从描述符中解析出的服务名称列表 |
| `compatibility_warnings` | `compatibility_warnings` | `string[]` | 检测到的兼容性警告（字段删除/类型变更等），不为空时建议检查 |
| `applied_version` | `applied_version` | `string` | 实际生效的版本号（可能与请求中不同） |
| `descriptor_hash` | `descriptor_hash` | `string` | 描述符内容的 SHA-256 哈希（hex 字符串） |

### 5.3 错误码说明

| gRPC Status | HTTP Status | 场景 |
|-------------|-------------|------|
| `INVALID_ARGUMENT` | 400 | 描述符格式错误、服务名不匹配、超过 10MB 限制 |
| `FAILED_PRECONDITION` | 412 | 乐观锁冲突（`previous_version` 与实际不符） |
| `UNAUTHENTICATED` | 401 | 缺少或无效的 API Key |
| `PERMISSION_DENIED` | 403 | API Key 无 `service:descriptor:write` scope |
| `DEADLINE_EXCEEDED` | 504 | ETCD 写入超时 |

---

## 七、RollbackDescriptor — 回滚描述符版本（业务侧可用）

**权限**：业务侧（通过 `app_secret` 认证，需 `service:<service_name>:rollback_descriptor` scope）  
**gRPC 方法**：`RollbackDescriptor`  
**HTTP**：`POST /api/v1/discovery/descriptors/{service_name}/rollback`

### 6.1 请求参数 `RollbackDescriptorRequest`

| 字段 | JSON Key | 类型 | 必填 | 说明 |
|------|----------|------|------|------|
| `service_name` | `service_name` | `string` | 是 | 服务完整名称 |
| `target_version` | `target_version` | `string` | 是 | 目标版本号，需已存在于版本历史中 |

### 6.2 响应参数 `RollbackDescriptorResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `success` | `success` | `bool` | 是否回滚成功 |
| `message` | `message` | `string` | 状态描述 |
| `active_version` | `active_version` | `string` | 回滚后当前生效版本号 |
| `discovered_services` | `discovered_services` | `string[]` | 回滚版本中的服务名称列表 |

---

## 八、ListDescriptorVersions — 列出版本历史

**权限**：所有认证用户（只读）  
**gRPC 方法**：`ListDescriptorVersions`  
**HTTP**：`GET /api/v1/discovery/descriptors/{service_name}/versions`

### 7.1 请求参数 `ListDescriptorVersionsRequest`

| 字段 | JSON Key | 类型 | 必填 | 说明 |
|------|----------|------|------|------|
| `service_name` | `service_name` | `string` | 是 | 服务完整名称 |

### 7.2 响应参数 `ListDescriptorVersionsResponse`

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `versions` | `versions` | `DescriptorVersionInfo[]` | 版本列表，见 [DescriptorVersionInfo](#descriptorversioninfo) |
| `active_version` | `active_version` | `string` | 当前生效版本号 |

#### DescriptorVersionInfo

| 字段 | JSON Key | 类型 | 说明 |
|------|----------|------|------|
| `version` | `version` | `string` | 版本标识 |
| `descriptor_hash` | `descriptor_hash` | `string` | 描述符 SHA-256 哈希 |
| `created_at` | `created_at` | `Timestamp` | 创建时间，RFC3339 格式 |
| `description` | `description` | `string` | 版本描述 |
| `services` | `services` | `string[]` | 该版本中包含的服务名称 |
| `size_bytes` | `size_bytes` | `uint64` | 描述符文件大小（字节） |
| `is_active` | `is_active` | `bool` | 是否为当前生效版本 |

---

## 九、嵌套对象定义

### LoadBalancer

| 字段 | JSON Key | 类型 | 必填 | 说明 |
|------|----------|------|------|------|
| `type` | `type` | `BalanceType (enum)` | 是 | 负载均衡算法，见 [BalanceType](#balancetype) |
| `endpoints` | `parameters` | `Endpoint[]` | 是 | 后端端点列表，至少一个 |

### Endpoint

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `address` | `address` | `string` | 是 | — | 后端 IP 或主机名 |
| `port` | `port` | `uint32` | 是 | — | 后端端口 |
| `weight` | `weight` | `uint32` | 否 | `1` | 流量权重，用于 WRR/WLC 算法 |

### HealthCheckConfig

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `enabled` | `enabled` | `bool` | 否 | `true` | 是否启用主动健康检查 |
| `grpc_method` | `grpc_method` | `string` | 否 | 自动探测 | gRPC 健康探测方法，如 `stew.api.v1.UserService/GetUser`，空则自动选第一个方法 |
| `http_path` | `http_path` | `string` | 否 | — | HTTP 健康检查路径，如 `/health`，与 `grpc_method` 二选一 |
| `interval_seconds` | `interval_seconds` | `uint32` | 否 | `30` | 探测间隔（秒） |
| `timeout_seconds` | `timeout_seconds` | `uint32` | 否 | `5` | 单次探测超时（秒） |
| `healthy_threshold` | `healthy_threshold` | `uint32` | 否 | `2` | 连续成功 N 次后标记为健康 |
| `unhealthy_threshold` | `unhealthy_threshold` | `uint32` | 否 | `3` | 连续失败 N 次后标记为不健康 |

### ServiceMiddlewareConfig

| 字段 | JSON Key | 类型 | 必填 | 默认值 | 说明 |
|------|----------|------|------|--------|------|
| `rate_limit_enabled` | `rate_limit_enabled` | `bool` | 否 | `true` | 是否为该服务启用限流 |
| `rate_limit_rpm` | `rate_limit_rpm` | `uint32` | 否 | `0` | 服务级每分钟请求上限，`0` 沿用全局配置 |
| `rate_limit_user_rpm` | `rate_limit_user_rpm` | `uint32` | 否 | `0` | 单用户每分钟请求上限，`0` 沿用全局配置 |
| `cors_enabled` | `cors_enabled` | `bool` | 否 | `false` | 是否启用 CORS |
| `cors` | `cors` | `ServiceCorsConfig` | 否 | — | CORS 详细配置，见下表 |
| `risk_enabled` | `risk_enabled` | `bool` | 否 | `false` | 是否启用风险评估 |
| `risk` | `risk` | `ServiceRiskConfig` | 否 | — | 风险评估配置 |
| `turnstile_enabled` | `turnstile_enabled` | `bool` | 否 | `false` | 是否启用 Cloudflare Turnstile 验证 |
| `turnstile` | `turnstile` | `ServiceTurnstileConfig` | 否 | — | Turnstile 配置 |

#### ServiceCorsConfig

| 字段 | JSON Key | 类型 | 默认值 | 说明 |
|------|----------|------|--------|------|
| `enabled` | `enabled` | `bool` | `false` | 启用 CORS |
| `allow_origins` | `allow_origins` | `string[]` | `[]` | 允许的来源，`["*"]` 表示全部 |
| `allow_methods` | `allow_methods` | `string[]` | `[]` | 允许的 HTTP 方法 |
| `allow_headers` | `allow_headers` | `string[]` | `[]` | 允许的请求头 |
| `expose_headers` | `expose_headers` | `string[]` | `[]` | 暴露给客户端的响应头 |
| `allow_credentials` | `allow_credentials` | `bool` | `false` | 是否允许携带 Cookie |
| `max_age_secs` | `max_age_secs` | `uint32` | `3600` | 预检请求缓存时间（秒） |

---

## 十、枚举值参考

### BalanceType

| 枚举值 | 整数值 | 说明 |
|--------|--------|------|
| `BALANCE_TYPE_UNKNOWN` | `0` | 未指定（不推荐） |
| `BALANCE_TYPE_ROUND_ROBIN` | `1` | 轮询（RR），适合同构后端 |
| `BALANCE_TYPE_WEIGHTED_ROUND_ROBIN` | `2` | 加权轮询（WRR），结合 `weight` 字段 |
| `BALANCE_TYPE_CONSISTENT_HASH` | `3` | 一致性哈希，会话粘滞场景 |
| `BALANCE_TYPE_LEAST_CONNECTIONS` | `4` | 最小连接数（LC） |
| `BALANCE_TYPE_SED` | `5` | 最短预期延迟（SED） |
| `BALANCE_TYPE_WEIGHTED_LEAST_CONNECTIONS` | `6` | 加权最小连接数（WLC） |
| `BALANCE_TYPE_NEVER_QUEUE` | `7` | 永不排队（NQ） |

### ServiceStatus

| 枚举值 | 整数值 | 说明 |
|--------|--------|------|
| `SERVICE_STATUS_UNKNOWN` | `0` | 未知 |
| `SERVICE_STATUS_HEALTHY` | `1` | 健康，参与流量分配 |
| `SERVICE_STATUS_UNHEALTHY` | `2` | 不健康，暂停流量分配 |
| `SERVICE_STATUS_MAINTENANCE` | `3` | 维护模式，停止接收新请求 |
| `SERVICE_STATUS_DRAINING` | `4` | 流量耗尽中，等待现有请求处理完毕 |

---

## 十一、完整请求示例

### 11.1 InitService — 初始化服务（管理端）

```json
POST /api/v1/discovery/services/init
Authorization: Bearer <admin_token>

{
  "service_name": "stew.api.v1.OrderService",
  "description": "Order management service",
  "protocol": "grpc"
}
```

**响应**：

```json
{
  "success": true,
  "message": "Service initialized successfully",
  "app_id": "key_abc123",
  "app_secret": "ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "service_name": "stew.api.v1.OrderService"
}
```

---

### 11.2 RegisterService — 最小化注册（管理端）

```json
POST /api/v1/discovery/services/register
Authorization: Bearer <admin_token>

{
  "service": {
    "service_name": "stew.api.v1.UserService",
    "instance_id": "user-svc-pod-abc123",
    "lb": {
      "type": "BALANCE_TYPE_ROUND_ROBIN",
      "parameters": [
        { "address": "10.0.0.5", "port": 50051 }
      ]
    },
    "version": "v1.2.3",
    "tags": { "env": "prod", "region": "cn-north-1" },
    "protocol": "grpc"
  },
  "ttl": 60
}
```

**响应**：

```json
{
  "success": true,
  "message": "Service registered successfully",
  "lease_id": "7587873756604589057"
}
```

---

### 11.3 RegisterService — 带描述符、健康检查和中间件配置（管理端）

```json
POST /api/v1/discovery/services/register
Authorization: Bearer <admin_token>

{
  "service": {
    "service_name": "stew.api.v1.OrderService",
    "instance_id": "order-svc-pod-xyz789",
    "lb": {
      "type": "BALANCE_TYPE_WEIGHTED_ROUND_ROBIN",
      "parameters": [
        { "address": "10.0.1.10", "port": 50051, "weight": 80 },
        { "address": "10.0.1.11", "port": 50051, "weight": 20 }
      ]
    },
    "version": "v2.0.0",
    "tags": { "env": "prod", "canary": "true" },
    "protocol": "grpc",
    "tls_enabled": false,
    "health_check_config": {
      "enabled": true,
      "grpc_method": "stew.api.v1.OrderService/HealthCheck",
      "interval_seconds": 15,
      "timeout_seconds": 3,
      "healthy_threshold": 2,
      "unhealthy_threshold": 3
    },
    "protobuf_descriptor": "<base64_encoded_pb_binary>",
    "middleware_config": {
      "rate_limit_enabled": true,
      "rate_limit_rpm": 500,
      "rate_limit_user_rpm": 60,
      "cors_enabled": false
    }
  },
  "ttl": 90
}
```

---

### 11.4 UploadProtobufDescriptor — 首次上传（业务侧）

```json
POST /api/v1/discovery/descriptors/stew.api.v1.OrderService/upload
x-api-key: <app_secret>

{
  "service_name": "stew.api.v1.OrderService",
  "descriptor_data": "<base64_encoded_pb_binary>",
  "descriptor_version": "20260318-a1b2c3d4",
  "description": "Order service v2.0.0 initial release",
  "force": false,
  "previous_version": ""
}
```

**响应**：

```json
{
  "success": true,
  "message": "Descriptor uploaded and stored: stew.api.v1.OrderService v20260318-a1b2c3d4. Loaded 1 service(s): [\"stew.api.v1.OrderService\"]",
  "descriptor_key": "/descriptors/stew.api.v1.OrderService/v/20260318-a1b2c3d4",
  "discovered_services": ["stew.api.v1.OrderService"],
  "compatibility_warnings": [],
  "applied_version": "20260318-a1b2c3d4",
  "descriptor_hash": "e3b0c44298fc1c149afb"
}
```

---

### 11.5 UploadProtobufDescriptor — 更新并使用乐观锁（业务侧）

```json
POST /api/v1/discovery/descriptors/stew.api.v1.OrderService/upload
x-api-key: <app_secret>

{
  "service_name": "stew.api.v1.OrderService",
  "descriptor_data": "<base64_encoded_new_pb_binary>",
  "descriptor_version": "20260318-b5c6d7e8",
  "description": "Add GetOrderStatistics RPC",
  "force": false,
  "previous_version": "20260318-a1b2c3d4"
}
```

---

### 11.6 RollbackDescriptor（业务侧）

```json
POST /api/v1/discovery/descriptors/stew.api.v1.OrderService/rollback
x-api-key: <app_secret>

{
  "service_name": "stew.api.v1.OrderService",
  "target_version": "20260318-a1b2c3d4"
}
```

**响应**：

```json
{
  "success": true,
  "message": "Descriptor rolled back to version '20260318-a1b2c3d4' for service 'stew.api.v1.OrderService'",
  "active_version": "20260318-a1b2c3d4",
  "discovered_services": ["stew.api.v1.OrderService"]
}
```

---

### 11.7 ListDescriptorVersions（只读）

```
GET /api/v1/discovery/descriptors/stew.api.v1.OrderService/versions
Authorization: Bearer <token>
```

**响应**：

```json
{
  "versions": [
    {
      "version": "20260318-b5c6d7e8",
      "descriptor_hash": "f4a1b2c3d4e5f6a7b8c9",
      "created_at": "2026-03-18T12:05:00Z",
      "description": "Add GetOrderStatistics RPC",
      "services": ["stew.api.v1.OrderService"],
      "size_bytes": 4096,
      "is_active": false
    },
    {
      "version": "20260318-a1b2c3d4",
      "descriptor_hash": "e3b0c44298fc1c149afb",
      "created_at": "2026-03-18T10:00:00Z",
      "description": "Order service v2.0.0 initial release",
      "services": ["stew.api.v1.OrderService"],
      "size_bytes": 3840,
      "is_active": true
    }
  ],
  "active_version": "20260318-a1b2c3d4"
}
```

---

### 11.8 UpdateServiceHealth — 心跳维活（业务侧）

```json
POST /api/v1/discovery/services/health
x-api-key: <app_secret>

{
  "service_name": "stew.api.v1.OrderService",
  "instance_id": "order-svc-pod-xyz789",
  "status": "SERVICE_STATUS_HEALTHY",
  "health_message": "all subsystems nominal"
}
```

---

### 11.9 DeregisterService — 优雅下线（管理端）

```json
POST /api/v1/discovery/services/deregister
Authorization: Bearer <admin_token>

{
  "service_name": "stew.api.v1.OrderService",
  "instance_id": "order-svc-pod-xyz789"
}
```

---

## 相关文档

- [业务接入指南](业务接入指南.md) — 完整接入流程与 SDK 快速入门
- [服务发现与热加载](服务发现与热加载.md) — 描述符版本化存储与热加载机制
- [认证接入指南](认证接入指南.md) — API Key 申请与认证配置
- [中间件](中间件.md) — 限流、CORS、风险评估中间件配置
